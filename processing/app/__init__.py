"""processing.app — 플랫폼의 모든 실행 진입점(CLI · HTTP API)을 모은 얇은 오케스트레이션 레이어.

**흐름에서의 위치**: 사람·스케줄러가 부르는 가장 바깥 껍데기다. 여기서 인자를 받고 환경을
차려 놓은 뒤 조합 레이어와 코어에 넘긴다 — **도메인 로직을 직접 들고 있지 않다**.

진입점
  · ``run_ingest``            — 파일 하나하나를 수집·적재(경로 판정 → 분류 → 추출 → 임베딩 → 저장)
  · ``run_relations``         — 자산 사이 관계 제안 배치
  · ``run_search``            — 검색 CLI
  · ``run_opensearch_resync`` — 색인 유실·드리프트 때 쓰는 **복구 도구**(정상 경로 아님)

기동 순서는 모두 같다: ``.env.{env}`` 로드 → 설정 동결 → DB 연결. 환경변수는 이미 설정된
쪽을 존중해 덮어쓰지 않는다(운영에서 주입한 값이 파일에 밀리면 안 된다).

⚠️ **적재·관계 진입점만** 전략 등록 모듈을 import 한다 — 그 import 가 곧 레지스트리를 채우는
부수효과다. 검색·복구는 전략도 도메인 팩도 쓰지 않아 부르지 않는다.
"""
